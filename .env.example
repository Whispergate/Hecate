# hecate/.env.example — copy to .env

# The address YOUR BROWSER uses to reach Mythic.
# This is baked into the JS bundle at build time.
# If you open Mythic at https://localhost:7443 → keep default.
# If Mythic is on another machine → set its IP/hostname here.
VITE_MYTHIC_HOST=localhost:7443
